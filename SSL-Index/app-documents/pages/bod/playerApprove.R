playerApproveUI <- function(id) {
  ns <- NS(id)
  tagList(
    "This page is blank"
  )
}

playerApproveServer <- function(id) {
  moduleServer(
    id,
    function(input, output, session) {
      
    }
  )
}