playerApproveUI <- function(id) {
  ns <- NS(id)
  tagList(
    "This page is blank"
  )
}

playerApproveServer <- function(id, userinfo) {
  moduleServer(
    id,
    function(input, output, session) {
      
    }
  )
}