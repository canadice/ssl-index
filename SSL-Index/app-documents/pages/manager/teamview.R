managerTeamUI <- function(id) {
  ns <- NS(id)
  tagList(
    "This page is blank"
  )
}

managerTeamServer <- function(id, userinfo) {
  moduleServer(
    id,
    function(input, output, session) {
      
    }
  )
}