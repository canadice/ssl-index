managerTeamUI <- function(id) {
  ns <- NS(id)
  tagList(
    "This page is blank"
  )
}

managerTeamServer <- function(id) {
  moduleServer(
    id,
    function(input, output, session) {
      
    }
  )
}